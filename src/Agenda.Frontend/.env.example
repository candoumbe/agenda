# Environment variables - Example

# Copy this file and rename it to .env, then adjust the values
# NEVER commit this file if it contains real secrets

# OpenID Connect / OAuth2 Authentication Configuration
AGENDA_AUTH_AUTHORITY=https://keycloak.example.com/auth/realms/agenda
AGENDA_AUTH_CLIENT_ID=agenda-frontend
AGENDA_AUTH_SCOPE=openid profile email
